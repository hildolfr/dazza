export default {
    channel: 'fatpizza',
    enabled: true,
    // Room-specific configuration overrides can be added here
    // For example:
    // greetingCooldown: 300000, // Override default greeting cooldown for this room
    // maxReconnectAttempts: 10, // Override default reconnect attempts
};